import { Meteor } from 'meteor/meteor';
import { Template } from 'meteor/templating';
// Corresponding html file
import './spinner.html';


Template.spinner.onCreated(() => {});


Template.spinner.helpers({});


Template.spinner.onRendered(() => {});


Template.spinner.events({});

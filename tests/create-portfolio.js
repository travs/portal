/* eslint-env mocha */
/* globals browser */

describe('list ui', () => {
  beforeEach(function () {
    browser.url('http://localhost:3000');
  });

  it('can create a list @watch', () => {
    
  });
});